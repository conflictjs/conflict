import Command from "./conflict/commands";

export const meta = {
    name: '/help',
    type: Command.Slash
}

export function Help () {
    return (
        <Meta>
            
        </Meta>
    )
}
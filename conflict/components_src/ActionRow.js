export default function ActionRow ({ children }) {
    return (
        <components_arr>
            <component type={1}>
                {children}
            </component>
        </components_arr>
    )
}